# soa-validate — M1 Plan (rewritten 2026-04-20, rev 2 post-evaluator)

**You are in `soa-validate`.** See repo-root `CONTEXT.md` for the history of decisions. This file is the authoritative tactical plan for this repo. If the spec repo lands a commit that touches a section, fixture, schema, or test ID referenced below, the spec session rewrites this file AND commits it under this repo BEFORE you pull.

## Spec pin (as of this plan version)

| Field | Value |
|---|---|
| spec_commit_sha | `80680cd` — L-19 (§10.3.2 decision recording) + L-20 (session schema sync) |
| spec_manifest_sha256 | `3fc46237667252572a76f85438433d69358cbf037d588ead16057720e21ad896` |
| M1 test set | **7 tests:** `HR-01, HR-12, HR-14, SV-CARD-01, SV-SIGN-01, SV-BOOT-01, SV-PERM-01`. `HR-02` deferred to M3 via must-map's `implementation_milestone: "M3"` — filter out in M1 runs. |
| New test IDs validator runs in M1 | `SV-AUDIT-TAIL-01, SV-AUDIT-RECORDS-01, SV-AUDIT-RECORDS-02, SV-SESS-BOOT-01, SV-SESS-BOOT-02, SV-PERM-20, SV-PERM-21, SV-PERM-22` — 15 total for the extension matrix |

## Done (2026-04-20 Week 3 day 2 closeout)

| Component | Status |
|---|---|
| Go CLI + must-map loader + JUnit writer | shipped Week 0 |
| JCS parity (`gowebpki/jcs` vs spec-generated vectors, 47/47) | shipped Week 0 |
| `internal/agentcard` + `internal/specvec` | shipped Week 1 |
| Runner HTTP client (mTLS + bearer + SSE) + TLS trust-anchor round trip | shipped Week 1 |
| Per-path Evidence model (vector + live) in JUnit output | shipped Week 1 |
| `internal/permresolve` — independent §10.3 oracle with 24-cell matrix validator | shipped Week 3 day 2 |
| `internal/toolregistry` — loader for pinned Tool Registry fixture | shipped Week 3 day 2 |

**Scoreboard at rev 2 writing time (per validator STATUS.md 2026-04-20 Week 3 day 2):**

| Test | vector | live | notes |
|---|---|---|---|
| SV-CARD-01 | pass | pass | Week 1 |
| SV-SIGN-01 | pass | pass | Week 1 |
| SV-BOOT-01 | — | pass | Week 2; happy path only |
| SV-PERM-01 | pass (2 items: PDA vector + oracle matrix) | skip | Week 3 day 2; live unblocks once impl ships the full endpoint set |

**Pin-bump pending:** still at `e7580b9`. Needs to move to `80680cd` to adopt §10.5.3 audit-records + §10.3.2 decisions + updated session schema + SV-PERM-20/21/22 catalog entries.

## Relabel task (blocking honest scoreboard)

Previous scoreboard had `HR-01 pass (vector)` and `HR-02 pass (vector)` entries. These tests were validating **initial-trust schema** and **CRL schema** negatives — legitimate work but NOT what HR-01 / HR-02 assert per must-map:

- must-map `HR-01` = "Destructive tool without Prompt → denied"
- must-map `HR-02` = "Projection over budget → StopReason::BudgetExhausted"

Action (do BEFORE rewiring live-path tests):
- [ ] Relabel the existing trust-schema negative tests: they belong under `SV-BOOT-01` negative path OR a new local test category (not HR-01). Keep the code, change the label.
- [ ] Relabel the existing CRL-schema negative tests: they're CRL validation coverage, not HR-02. Keep the code, change the label.
- [ ] Mark HR-02 as `skip` with reason `implementation_milestone=M3` reading the must-map.
- [ ] The new HR-01 assertion path is in 3.4 below.

## Week 3 day 3+ remaining work

### V-01 — Adopt spec pin `80680cd`

- [ ] Pull the spec repo to `80680cd`
- [ ] Update `soa-validate.lock`:
  ```
  spec_commit_sha      = 80680cd<full sha>
  spec_commit_date     = 2026-04-20
  spec_manifest_sha256 = 3fc46237667252572a76f85438433d69358cbf037d588ead16057720e21ad896
  pin_history append:
    reason = "adopt L-13 catalog integration + L-14 HR-02 M3 deferral +
             L-15 audit records + L-16/L-17/L-18 fixtures +
             L-19 permission decisions + L-20 session-schema sync
             (commits 8b35375 + 80680cd and precedents)"
  ```

### V-02 — Schema registry update

Load into `internal/schemas` (or wherever the validator's JSON Schema registry lives):

- [ ] `schemas/permissions-resolve-response.schema.json` (already loaded at rev 1? verify)
- [ ] `schemas/audit-tail-response.schema.json` (new for V-02)
- [ ] `schemas/audit-records-response.schema.json` (new for V-02)
- [ ] `schemas/session-bootstrap-response.schema.json` (new for V-02)
- [ ] `schemas/permission-decision-request.schema.json` (new in commit `80680cd`) — for request-body construction sanity checks
- [ ] `schemas/permission-decision-response.schema.json` (new in commit `80680cd`) — for response validation
- [ ] `schemas/session.schema.json` (UPDATED in `80680cd`) — refresh cached compiled validator; now requires `activeMode`

### V-03 — SV-PERM-01 full 24-cell live sweep

Depends on impl shipping: POST /sessions (done), GET /permissions/resolve (done), GET /audit/tail (done), `RUNNER_CARD_FIXTURE` support (T-04 on impl side, pending), `RUNNER_TOOLS_FIXTURE` support (done).

- [ ] Wait for impl STATUS signal that T-04 (`RUNNER_CARD_FIXTURE` loading the DangerFullAccess conformance card) is live
- [ ] Live-path handler:
  1. POST /sessions × 3 with `requested_activeMode ∈ {ReadOnly, WorkspaceWrite, DangerFullAccess}`. Under the conformance card (DFA), all three MUST succeed with matching `granted_activeMode`. Store three `(session_id, session_bearer, granted_activeMode)` triples.
  2. GET /audit/tail → capture baseline `this_hash` (expected `"GENESIS"` on a fresh Runner)
  3. Iterate the 24-cell matrix (8 tools × 3 activeModes). For each cell: `GET /permissions/resolve?tool=<name>&session_id=<matching>`; validate response against `permissions-resolve-response.schema.json`; assert `decision` equals the oracle output (already verified against spec's README table via `TestOracleMatchesSpec24CellMatrix`)
  4. GET /audit/tail → capture post-sweep `this_hash`; assert **equal** to baseline (not-a-side-effect property from §10.3.1)
- [ ] Evidence model emits `SV-PERM-01` with two evidence records: `vector` and `live`, aggregated per the JUnit writer

### V-04 — SV-SESS-BOOT-01 / SV-SESS-BOOT-02

- [ ] SV-SESS-BOOT-01: POST /sessions with each of three activeMode values (within DFA card bounds); schema-validate the 201 body; assert `granted_activeMode == requested_activeMode`; session_id matches `^ses_[A-Za-z0-9]{16,}$`; session_bearer is non-empty opaque
- [ ] SV-SESS-BOOT-02: run against the `test-vectors/agent-card.json` fixture (activeMode=ReadOnly — default card, NOT conformance card); POST /sessions with `requested_activeMode=DangerFullAccess`; assert `403 Forbidden` with `reason="ConfigPrecedenceViolation"`

### V-05 — SV-AUDIT-TAIL-01

- [ ] Against fresh Runner (audit log empty): GET /audit/tail; schema-validate; assert `this_hash == "GENESIS"`, `record_count == 0`, `last_record_timestamp` is **omitted** (not null, not empty string)
- [ ] After driving any audit-write activity (via V-07 below): GET /audit/tail; assert `this_hash` is 64-char lowercase hex; `record_count >= 1`; `last_record_timestamp` present
- [ ] Not-a-side-effect regression: read twice back-to-back; both responses byte-identical

### V-06 — SV-AUDIT-RECORDS-01 / SV-AUDIT-RECORDS-02

Depends on V-07 having driven at least N=150 records so pagination is meaningfully tested.

- [ ] SV-AUDIT-RECORDS-01 (schema + pagination):
  - [ ] GET /audit/records → validate response against schema
  - [ ] Default limit=100; paginate through with `after=<next_after>` until `has_more=false`
  - [ ] Assert records in chain order (earliest first)
  - [ ] Assert pagination limit respected (`records.length <= limit`)
  - [ ] Assert `after` with an unknown id returns 404
  - [ ] Assert bearer without `audit:read` scope returns 403
- [ ] SV-AUDIT-RECORDS-02 (chain integrity):
  - [ ] Paginate to collect all records
  - [ ] Assert `records[0].prev_hash == "GENESIS"`
  - [ ] Assert `∀ i > 0: records[i].prev_hash == records[i-1].this_hash`
  - [ ] Validator-side tamper test: mutate `records[1].prev_hash` in the local copy; re-run chain verification; assert failure with specific break-at-index-1

### V-07 — Audit-record driver (common fixture for V-05/V-06/HR-14)

New Go helper that drives real audit records using the §10.3.2 endpoint:

- [ ] POST /sessions with `request_decide_scope: true` (impl grants `permissions:decide:<session_id>` scope — T-03 on impl side)
- [ ] Loop N=150 times:
  - [ ] Pick a ReadOnly/AutoAllow tool from the pinned Tool Registry fixture (e.g., `fs__read_file`, `fs__list_directory`, `mem__recall`)
  - [ ] POST /permissions/decisions with `{tool, session_id, args_digest: "sha256:0000…0000", pda: null}` (AutoAllow tools don't need a PDA)
  - [ ] Validate 201 response against `permission-decision-response.schema.json`
  - [ ] Assert `decision == "AutoAllow"`, `handler_accepted == true`, `audit_this_hash` is 64-char hex
- [ ] After loop: GET /audit/tail; assert `record_count == 150`, `this_hash` equals the last POST's `audit_this_hash`

### V-08 — SV-PERM-20 / SV-PERM-21 / SV-PERM-22

- [ ] SV-PERM-20 (endpoint + scope + audit write):
  - [ ] POST /permissions/decisions with valid body for an AutoAllow tool → 201; response body conforms to schema; audit record count incremented by exactly 1
  - [ ] POST /permissions/decisions with bearer lacking `permissions:decide:<session_id>` scope → 403
  - [ ] Forgery resistance: assert response `decision` equals what `/permissions/resolve` returned for the same `(tool, session_id)`
- [ ] SV-PERM-21 (PDA verify happy path):
  - [ ] Pick a tool whose resolver output is Prompt (e.g., `net__http_get` from the pinned registry — ReadOnly/Prompt)
  - [ ] Construct a signed PDA whose `canonical-decision.decision = "Prompt"` using a test handler keypair registered in the conformance card's trust anchors **(if conformance card template doesn't yet pin a handler key, flag as spec-repo follow-up L-21; for M1 validator may use the pinned `test-vectors/permission-prompt/pda.jws` if the conformance card's trust anchors include its signer; otherwise this test is SKIP with a documented gap)**
  - [ ] POST /permissions/decisions with that PDA → 201; assert `handler_accepted == true`; audit row's `signer_key_id` equals PDA `x5c[0]` kid
- [ ] SV-PERM-22 (PDA verify negative paths):
  - [ ] Bad signature: submit a PDA whose signature is flipped (easy: reuse tampered-card all-zeros pattern on a PDA) → 201 `decision=Deny handler_accepted=false reason=pda-verify-failed`; audit row IS written
  - [ ] Decision mismatch: submit a PDA whose `canonical-decision.decision` is `AutoAllow` when resolver computes `Prompt` → 403 `reason=pda-decision-mismatch`; NO audit row written

### V-09 — HR-12 (tampered card boot-time rejection)

- [ ] Invoke impl as subprocess with environment: `RUNNER_CARD_JWS=<spec-repo>/test-vectors/tampered-card/agent-card.json.tampered.jws` + `RUNNER_CARD_FIXTURE=<spec-repo>/test-vectors/conformance-card/agent-card.json` (or the default card — either works for this test since the JWS is tampered independent of card contents)
- [ ] Assert within 5 seconds: process exited non-zero with `CardSignatureFailed reason=signature-invalid` on stderr/log; OR process stayed up but `/ready` returned 503 with reason `card-signature-invalid`
- [ ] Regression: restart impl without the env var; assert `/ready=200`; confirms the verification path isn't generally broken

### V-10 — HR-14 (audit chain tamper — validator-side)

- [ ] Use V-07 to drive N records
- [ ] Paginate `/audit/records` until done; collect full chain
- [ ] Verify chain integrity (as V-06 already does)
- [ ] Tamper test: mutate `records[N/2].prev_hash` in local copy; re-run chain-integrity check; assert **failure** with specific break index
- [ ] Do NOT mutate on the impl side; the impl's audit log stays honest. HR-14's test is on validator's chain verifier, not on impl's tamper resistance

### V-11 — HR-01 (Destructive without Prompt → Deny)

- [ ] Against DFA-card Runner with pinned Tool Registry loaded:
  - [ ] `proc__spawn_shell` is `risk_class=Destructive, default_control=Deny` in the pinned fixture
  - [ ] `GET /permissions/resolve?tool=proc__spawn_shell&session_id=<DFA session>` → assert `decision="Deny"` with reason `deny-by-control`
  - [ ] `GET /permissions/resolve?tool=proc__spawn_shell&session_id=<ReadOnly session>` → assert `decision="CapabilityDenied"` with reason `risk-class-not-permitted-under-capability`
  - [ ] `GET /permissions/resolve?tool=proc__spawn_shell&session_id=<WorkspaceWrite session>` → assert `decision="CapabilityDenied"` (same reason)
- [ ] Literal must-map text: "Destructive tool without Prompt → denied" — asserted across all three capability levels

### V-12 — SV-BOOT-01 negative paths

- [ ] Invoke impl subprocess with `RUNNER_INITIAL_TRUST=<spec-repo>/test-vectors/initial-trust/mismatched-publisher-kid.json` + default card; assert exit non-zero with `HostHardeningInsufficient reason=bootstrap-missing` within 5s
- [ ] Same pattern with `expired.json` → `bootstrap-expired`
- [ ] Same pattern with `channel-mismatch.json` → `bootstrap-invalid-schema`
- [ ] Regression: restart impl with `valid.json`; assert `/ready=200`

### V-13 — Platform matrix regression

- [ ] `release-gate.json` v2 emits `per_platform[]` evidence when the validator is run on multiple platforms sequentially (CI matrix). Linux/macOS/Windows green on all 15 M1-plus-extension test IDs is the gate.

## Week 4 — Full 15-test regression against `npx create-soa-agent demo` Runner

- [ ] Once impl completes Week 5b, switch validator's target from a dev-loop Runner to a scaffold-spawned Runner; re-run the full suite; assert identical green
- [ ] Emit final `release-gate.json` with cold-cache boot timings per platform

## Week 5 — M1 exit sign-off

- [ ] STATUS.md: "Week 3 day 3+ through M1 complete; 7 M1 + 8 extension test IDs green on both paths across 3 platforms; HR-02 correctly M3-deferred; pin at `<final commit>`"
- [ ] `soa-validate --profile=core --impl-url=http://localhost:7700 --spec-vectors=<spec>` emits JUnit XML with per-path evidence and `release-gate.json` with pass/fail per test ID

## Deferred to later milestones

- HR-02 — M3 (§13 Token Budget dependency; filter out by `implementation_milestone` field)
- SV-PERM-19 (audit three-state degradation) — M2
- SV-SESS-01..10 (session persistence + resume live tests) — M2
- Everything in phases 4 (Self-Improvement) and 5 (Handoff) of the must-map — M4/M5

## Pinned fixtures this plan consumes (from spec repo at pinned commit)

| Fixture path | Loaded in validator | Purpose | Test IDs |
|---|---|---|---|
| `test-vectors/agent-card.json` + `.jws` | `internal/specvec` | SV-CARD-01/SV-SIGN-01 structural vectors | SV-CARD-01, SV-SIGN-01 |
| `test-vectors/conformance-card/agent-card.json` | used indirectly (validator asks impl to load it) | DFA card for three-session sweep | SV-PERM-01, SV-SESS-BOOT-01 |
| `test-vectors/tampered-card/agent-card.json.tampered.jws` | subprocess env var to impl | Invalid-signature fixture | HR-12 |
| `test-vectors/initial-trust/*.json` | subprocess env var to impl | Bootstrap positive + negative cases | SV-BOOT-01 (all paths) |
| `test-vectors/crl/*.json` | used via clock-injected tests | CRL state-machine fixtures | secondary |
| `test-vectors/tool-registry/tools.json` | `internal/toolregistry` + oracle | 24-cell expected decision matrix | SV-PERM-01, SV-PERM-20/21/22 |
| `test-vectors/permission-prompt/*` | `internal/permprompt` | PDA-JWS + canonical-decision | SV-PERM-01 vector, SV-PERM-21 |

## Schemas the validator loads (pinned at spec commit `80680cd`)

| Schema | Used by |
|---|---|
| `agent-card.schema.json` | SV-CARD-01 vector |
| `initial-trust.schema.json` | SV-BOOT-01 schema-negative |
| `crl.schema.json` | CRL fixture validation |
| `canonical-decision.schema.json` | SV-PERM-01 vector |
| `session.schema.json` (**updated in `80680cd` to require activeMode**) | session-file sanity |
| `permissions-resolve-response.schema.json` | SV-PERM-01 live |
| `permission-decision-request.schema.json` (**NEW**) | V-07 audit-record driver request construction |
| `permission-decision-response.schema.json` (**NEW**) | SV-PERM-20/21/22, V-07 audit-record driver |
| `audit-tail-response.schema.json` | SV-AUDIT-TAIL-01, SV-PERM-01 not-a-side-effect |
| `audit-records-response.schema.json` | SV-AUDIT-RECORDS-01/02, HR-14 |
| `session-bootstrap-response.schema.json` | SV-SESS-BOOT-01/02 |

## Plan refresh triggers (spec session's commitment)

This file is rewritten by the spec session AND committed under this repo BEFORE validator pulls when the spec touches:
- §5.3 / §5.3.1 / §5.4 / §6.1 / §6.1.1
- §10.3 / §10.3.1 / §10.3.2 / §10.5 / §10.5.1 / §10.5.2 / §10.5.3 / §10.6 / §10.6.1
- §11 / §12 / §12.1 / §12.6 / §15 / §15.5
- Any fixture in `test-vectors/` listed above
- Any schema in `schemas/` listed above
- `soa-validate-must-map.json` — any test-ID milestone/section/severity/assertion change
- Inline-schema ↔ standalone-schema drift (the L-20 incident pattern)

## Daily status protocol

End of day: update `STATUS.md` at repo root with:
```
## <date>
- Done: <complete>
- Active: <in-flight>
- Blocked: <cross-session ask for spec or impl>
- Pin: <current spec_commit_sha>
- Scoreboard: <per-test-id vector/live status>
```

Commit and push.
