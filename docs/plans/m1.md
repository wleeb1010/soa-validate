# soa-validate — M1 Plan (rewritten 2026-04-20)

**You are in `soa-validate`.** See repo-root `CONTEXT.md` for the history of decisions. This file is the authoritative tactical plan for this repo. If the spec repo lands a commit that touches a section referenced below, the spec session rewrites this file before the validator session acts on it.

## Spec pin (as of this plan version)

| Field | Value |
|---|---|
| spec_commit_sha | `8b35375` |
| M1 test set | **7 tests** (not 8): `HR-01, HR-12, HR-14, SV-CARD-01, SV-SIGN-01, SV-BOOT-01, SV-PERM-01` |
| Deferred to M3 | `HR-02` — requires §13 Token Budget. Must-map tags HR-02 with `implementation_milestone: "M3"`. |
| New test IDs validator runs in M1 (catalog-integrated this week) | `SV-AUDIT-TAIL-01, SV-AUDIT-RECORDS-01, SV-AUDIT-RECORDS-02, SV-SESS-BOOT-01, SV-SESS-BOOT-02` |

## Done (2026-04-20 Week 3 day 2 closeout)

| Test | Status | Evidence |
|---|---|---|
| SV-CARD-01 | vector + live green | schema validation + Cache-Control + ETag + http:// rejection |
| SV-SIGN-01 | vector + live green | detached JWS envelope + typ + x5c |
| SV-BOOT-01 | live green (happy path) | /health + /ready against a healthy Runner |
| SV-PERM-01 | vector green | pinned permission-prompt/ bundle schema + digest |
| HR-01 | mis-labeled (was testing initial-trust schema) | **relabel in this plan** — initial-trust negative tests are SV-BOOT-01 territory, not HR-01 |
| HR-02 | mis-labeled (was testing CRL schema) | **deferred to M3** + the CRL-schema tests relabel as separate test IDs |

**Relabeling note.** Validator's previous "HR-01 pass (negative)" and "HR-02 pass (negative)" tests were actually schema-validation tests for `initial-trust.schema.json` and `crl.schema.json` respectively. Per must-map:
- HR-01 = "Destructive tool without Prompt → denied"
- HR-02 = "Projection over budget → StopReason::BudgetExhausted"

Neither matches what was being tested. The schema-validation work is valuable — relabel the existing tests as either (a) coverage under SV-BOOT-01 negative path + SV-PERM-XX CRL assertions, or (b) new test IDs in the spec's SV-TRUST / SV-CRL category (follow-up request to the spec session if needed). Do NOT delete the existing fixture-parse tests; just stop labeling them HR-01/HR-02.

## Week 3 day 3 onward

### 3.3 — Schema adoption for three new endpoints

- [ ] Load into schema registry:
  - `schemas/permissions-resolve-response.schema.json` (from `2eccf6e`)
  - `schemas/audit-tail-response.schema.json` (from `e7580b9`)
  - `schemas/audit-records-response.schema.json` (from `e8b4e9e`)
  - `schemas/session-bootstrap-response.schema.json` (from `e7580b9`)
- [ ] Compile each via `santhosh-tekuri/jsonschema/v5`; unit-test round-trip of a sample body per schema

### 3.4 — SV-PERM-01 full 24-cell live sweep

- [ ] Wait for impl to signal:
  1. `/permissions/resolve` endpoint live (already shipped)
  2. `POST /sessions` endpoint live (already shipped)
  3. `/audit/tail` endpoint live (already shipped)
  4. `RUNNER_CARD_FIXTURE` support live (Week 3 day 3 impl task)
  5. `RUNNER_TOOLS_FIXTURE` support live (Week 3 day 3 impl task)
  6. `SOA_RUNNER_BOOTSTRAP_BEARER` support live (Week 3 day 3 impl task)
- [ ] Once all 6 gates are live, run the 24-cell sweep:
  1. POST /sessions × 3 (one per activeMode: ReadOnly, WorkspaceWrite, DangerFullAccess) using the bootstrap bearer. Store three `(session_id, session_bearer, granted_activeMode)` triples.
  2. GET /audit/tail → capture baseline `this_hash`.
  3. For each of 24 cells (8 tools × 3 activeModes in `test-vectors/tool-registry/tools.json`'s matrix): `GET /permissions/resolve?tool=<name>&session_id=<matching-session>`. Assert `response.decision` matches the 24-cell expected matrix in `test-vectors/tool-registry/README.md`.
  4. GET /audit/tail → capture post-sweep `this_hash`. Assert **equal** to baseline (not-a-side-effect property).
- [ ] Assert specific cells for SV-PERM-01's must-map text: (ReadOnly × Mutating) and (ReadOnly × Destructive) and (WorkspaceWrite × Destructive) all yield `decision=CapabilityDenied`.

### 3.5 — SV-AUDIT-TAIL-01, SV-SESS-BOOT-01, SV-SESS-BOOT-02

- [ ] **SV-AUDIT-TAIL-01:** schema-validate `/audit/tail` response; test empty-log → this_hash=="GENESIS" and last_record_timestamp omitted; test non-empty log → this_hash is 64-char hex and record_count ≥ 1.
- [ ] **SV-SESS-BOOT-01:** POST /sessions with each of the three activeMode values (within card bounds). Schema-validate the 201 response; assert `granted_activeMode == requested_activeMode`; assert `session_id` matches `^ses_[A-Za-z0-9]{16,}$`; assert `session_bearer` is non-empty opaque.
- [ ] **SV-SESS-BOOT-02:** against the `test-vectors/agent-card.json` fixture (activeMode=ReadOnly), attempt POST /sessions with `requested_activeMode=DangerFullAccess`. Assert `403 Forbidden` with `reason="ConfigPrecedenceViolation"`.

### 3.6 — HR-12 (tampered card) live test

- [ ] Invoke impl binary as a subprocess with `RUNNER_CARD_JWS=<path-to-tampered-card-fixture>` env var pointing at `test-vectors/tampered-card/agent-card.json.tampered.jws`.
- [ ] Assert either: (a) process exits non-zero with CardSignatureFailed reason=signature-invalid on stderr/log; OR (b) process stays up but `/ready` returns 503 with reason `card-signature-invalid` within 5s of startup.
- [ ] Cleanup: kill subprocess.
- [ ] Regression: re-run the same binary with the valid fixture, assert /ready=200. Confirms verification path isn't generally broken.

### 3.7 — HR-14 (audit chain tamper) validator-side test

- [ ] Drive N permission decisions through the Runner (via POST /sessions + a tool-invocation path — see below).
- [ ] GET /audit/records paginated until has_more=false; collect all records.
- [ ] Verify chain integrity: `records[0].prev_hash == "GENESIS"` AND `∀ i > 0: records[i].prev_hash == records[i-1].this_hash`. Assert pass.
- [ ] Tamper test: mutate `records[1].prev_hash` in the local copy, re-run chain verification, assert **fail** with specific chain-break at index 1.
- [ ] Note: no tamper endpoint exists on impl — the tamper is validator-side, the Runner only serves the real chain.
- [ ] **Tool-invocation driving:** if no HTTP tool-invocation endpoint exists yet, drive prompts via impl's internal mechanism (integration harness) — flag as L-19 follow-up if this becomes a blocker. HR-14 can initially run against a synthetic pre-populated audit log for chain-integrity verification; tamper-detection logic is what's being tested, not the act of writing real records.

### 3.8 — SV-BOOT-01 negative path

- [ ] Invoke impl subprocess with `RUNNER_INITIAL_TRUST=<path-to-mismatched-publisher-kid.json>` pointed at the spec fixture + standard valid card.
- [ ] Assert process exits non-zero with `HostHardeningInsufficient reason=bootstrap-missing` within 5s of startup.
- [ ] Pair with existing SV-BOOT-01 happy-path test (already green) — the negative case proves the gate actually rejects, not just accepts-everything.

### 3.9 — SV-AUDIT-RECORDS-01 + SV-AUDIT-RECORDS-02

- [ ] **SV-AUDIT-RECORDS-01:** schema-validate `/audit/records` response; drive ≥ 150 decisions; paginate through; assert `next_after` and `has_more` transition correctly; assert records returned in chain order (earliest first); assert pagination respects `limit` parameter.
- [ ] **SV-AUDIT-RECORDS-02:** same drive + paginated read as above; verify `records[0].prev_hash == "GENESIS"` and `records[i].prev_hash == records[i-1].this_hash` for all i > 0. 

### Week 4 — HR-01 live assertion + full Week 3 regression

- [ ] **HR-01:** query `/permissions/resolve?tool=proc__spawn_shell&session_id=<any-mode-session>` (from the pinned Tool Registry fixture, `proc__spawn_shell` has `risk_class=Destructive, default_control=Deny`). Assert `decision=Deny` when activeMode=DangerFullAccess, and `decision=CapabilityDenied` for the other two modes. This is the literal must-map assertion: "Destructive tool without Prompt → denied."

### Week 5 — Full 7-test suite regression against final impl build

- [ ] All 7 M1 tests green on both vector AND live paths in one `soa-validate --profile=core --spec-vectors=... --impl-url=...` run
- [ ] JUnit XML emits per-path evidence (vector/live) for every test ID
- [ ] `release-gate.json` v2 includes `impl_url`, `live_path_enabled`, per-result `evidence[]`

### Week 6 — M1 exit gate

- [ ] Validator runs against a `create-soa-agent`-spawned Runner (not just a dev-loop Runner) on all 3 platforms: Linux, macOS, Windows
- [ ] Green on all 3; cold-cache impl boot time is recorded per platform
- [ ] `STATUS.md`: "M1 complete; 7/7 M1 test IDs green; HR-02 correctly deferred to M3 per L-14; validator + impl in lockstep pin"

## Deferred to later milestones

- **HR-02** (Token Budget) → M3 per L-14; skip during M1 filter
- **SV-PERM-19** (audit three-state degradation) → M2
- **SV-SESS-01..10** (session persistence + resume) → M2/M3
- Everything in phases 4 and 5 of the must-map → M4/M5

## Pinned fixtures this plan consumes (from spec repo at pinned commit)

| Fixture path | Purpose | Test IDs |
|---|---|---|
| `test-vectors/agent-card.json` + `.jws` | SV-CARD-01 / SV-SIGN-01 structural vectors | SV-CARD-01, SV-SIGN-01 |
| `test-vectors/conformance-card/agent-card.json` | DangerFullAccess card for session-bootstrap sweep | SV-PERM-01 |
| `test-vectors/tampered-card/agent-card.json.tampered.jws` | Invalid-signature fixture | HR-12 |
| `test-vectors/initial-trust/*.json` | Bootstrap positive + negative cases | SV-BOOT-01 (both paths) |
| `test-vectors/crl/*.json` | CRL state-machine fixtures | indirect |
| `test-vectors/tool-registry/tools.json` + README | 24-cell expected-decision matrix | SV-PERM-01 |
| `test-vectors/permission-prompt/*` | PDA-JWS and canonical-decision | SV-PERM-01 vector |

## Schemas to load (ajv or gojsonschema on validator side)

| Schema | Consumed by |
|---|---|
| `schemas/agent-card.schema.json` | SV-CARD-01 vector |
| `schemas/initial-trust.schema.json` | SV-BOOT-01 |
| `schemas/crl.schema.json` | CRL fixtures |
| `schemas/canonical-decision.schema.json` | SV-PERM-01 vector |
| `schemas/permissions-resolve-response.schema.json` | SV-PERM-01 live |
| `schemas/audit-tail-response.schema.json` | SV-AUDIT-TAIL-01, SV-PERM-01 live |
| `schemas/audit-records-response.schema.json` | SV-AUDIT-RECORDS-01, SV-AUDIT-RECORDS-02, HR-14 |
| `schemas/session-bootstrap-response.schema.json` | SV-SESS-BOOT-01, SV-SESS-BOOT-02 |
| `schemas/release-manifest.schema.json` | MANIFEST gate |

## Environment variables validator expects impl to honor

| Variable | Purpose |
|---|---|
| `SOA_RUNNER_BOOTSTRAP_BEARER` | Fixed bootstrap bearer for POST /sessions (loopback-only guard on impl side) |
| `RUNNER_CARD_FIXTURE=<path>` | Conformance DangerFullAccess card |
| `RUNNER_TOOLS_FIXTURE=<path>` | Conformance Tool Registry |
| `RUNNER_CARD_JWS=<path>` | Tampered card for HR-12 |
| `RUNNER_INITIAL_TRUST=<path>` | Mismatched-publisher-kid for SV-BOOT-01 negative |
| `RUNNER_TEST_CLOCK=<iso>` | Clock injection per L-01 |

## Plan refresh triggers

This file is rewritten when the spec repo lands a commit touching any of:
- §5.3 / §5.4 / §6.1 / §6.1.1
- §10.3 / §10.3.1 / §10.5 / §10.5.1 / §10.5.2 / §10.5.3 / §10.6 / §10.6.1
- §11 / §12 / §12.6 / §15.5
- Any fixture in the pinned-fixtures table above
- `soa-validate-must-map.json` — if a test ID in the M1 set changes milestone, section, severity, or assertion

The spec session detects the change at commit time and pushes a fresh plan BEFORE the validator session pulls.

## Daily status protocol

End of day: update `STATUS.md` with:
```
## <date>
- Done: <complete>
- Active: <in-flight>
- Blocked: <cross-session ask for spec or impl>
- Pin: <current spec_commit_sha>
- Scoreboard: <per-test vector/live status>
```

Commit and push.
