# soa-validate — M2 Plan (Crash-Safe Persistence)

**You are in `soa-validate`.** Spec-session-authored plan for M2. Builds on M1 exit state (15 pass / 1 skip / 0 fail, pin `8624a7a`, validator has internal/subprocrunner + §10.3 oracle + V-07 audit driver + internal/auditchain) without regressing any of it.

## Spec pin (M2 kickoff)

| Field | Value |
|---|---|
| spec_commit_sha | `6566707` — L-27 M2 kickoff |
| spec_manifest_sha256 | `9225b7e860034a5df2697190a8478ed0446c96407f989d7a87ca17650577daca` |
| M2 test set | **8 tests:** `HR-04, HR-05, SV-SESS-01, SV-SESS-02, SV-SESS-03, SV-SESS-04, SV-SESS-05, SV-PERM-19` + new `SV-SESS-STATE-01` (§12.5.1 endpoint) |
| M1 tests must stay green | all 15 M1 — don't regress |
| Deferred | HR-02 → M3 per implementation_milestone tag |

## M2 scope — remaining tasks

### V2-01 — Pin-bump to `6566707`

- [ ] Update `soa-validate.lock`:
  ```
  spec_commit_sha      = 6566707<full sha>
  spec_commit_date     = 2026-04-21
  spec_manifest_sha256 = 9225b7e860034a5df2697190a8478ed0446c96407f989d7a87ca17650577daca
  pin_history append:
    reason = "M2 kickoff: adopt L-27 §12.5.1 session state
              observability + §12.5.2 audit-sink failure simulation
              hook + tool-registry-m2 non-idempotent fixture. 8 new
              M2 test IDs tagged; catalog 221→222 with
              SV-SESS-STATE-01 added."
  ```

### V2-02 — Schema registry update

- [ ] Load into validator's schema registry: `schemas/session-state-response.schema.json`
- [ ] Confirm `schemas/session.schema.json` is the M1 L-20 version (required activeMode)

### V2-03 — V-14 session-state observer (§12.5.1)

- [ ] Go helper calling `GET /sessions/<id>/state` with a validator-obtained session bearer
- [ ] Schema-validate the response body
- [ ] Parse `workflow.side_effects[]` into a typed structure
- [ ] Ability to read the same session at multiple timepoints and diff the state (needed for V-16 bracket-persist and V-17 crash-recovery tests)
- [ ] Idempotency check: two reads in rapid succession return byte-identical bodies (not-a-side-effect from §12.5.1)

### V2-04 — V-15 crash-test harness

- [ ] Extend `internal/subprocrunner` with kill-at-marker capability: watch impl stderr for a deterministic boundary marker (emitted by impl with `RUNNER_CRASH_TEST_MARKERS=1`), kill -9 at the named marker, verify subprocess exit
- [ ] Relaunch impl against the same session directory; wait for /ready=200
- [ ] Platform-aware: SIGKILL on Linux/macOS, process termination on Windows
- [ ] Documented platform limitations (per impl M2-T7: Windows may defer to v1.1 if Hyper-V simulated-crash proves unreliable); validator plan accepts platform parity gaps and labels accordingly

### V2-05 — SV-SESS-STATE-01 live

- [ ] Mint a session via `POST /sessions`
- [ ] Drive 2-3 permission decisions through `POST /permissions/decisions` (V-07 driver from M1)
- [ ] Call `GET /sessions/<id>/state`
  - Assert schema conformance
  - Assert `workflow.side_effects.length` matches the drive count
  - Assert each side_effect has required fields (tool, idempotency_key, phase ∈ {pending, inflight, committed, compensated}, args_digest)
- [ ] Not-a-side-effect regression: two reads back-to-back are byte-identical
- [ ] 404 on unknown session_id
- [ ] 403 on bearer without sessions:read scope
- [ ] 429 on rate-limit exhaustion (use deterministic loop past 120 rpm)

### V2-06 — HR-04 (pending replays idempotently) + HR-05 (committed does NOT replay)

- [ ] Launch impl with crash-test markers enabled
- [ ] Drive a tool invocation that triggers a side_effect (use `compliant_ephemeral_tool` from the M2 fixture once registered, or `fs__write_file` from M1 registry)
- [ ] **HR-04 path:**
  - Read /state just after `phase=pending` marker; capture `idempotency_key_pre`
  - Kill impl between pending-write and commit-write
  - Relaunch impl; wait for /ready
  - Read /state; observe either `phase=committed` with same `idempotency_key_post == idempotency_key_pre` (tool accepted the replay), OR still `phase=pending` with `first_attempted_at` preserved (tool is mid-retry)
  - Assert idempotency_key unchanged across the kill boundary
  - Tool-side counter: exactly 1 observed invocation (the dedupe path hit the tool's cache)
- [ ] **HR-05 path:**
  - Read /state just after `phase=committed` marker; capture state
  - Kill impl post-commit
  - Relaunch; /ready
  - Read /state; observe `phase=committed` unchanged
  - Tool-side counter: still exactly 1 observed invocation (replay did NOT fire)

### V2-07 — SV-SESS-03 bracket-persist observed for every significant event

- [ ] Drive N=10 significant events (mix of tool invocations + permission decisions)
- [ ] Poll /state during the drive, capture every state snapshot where `side_effects[].phase` changes
- [ ] Assert: every side_effect's phase transitions go pending → committed OR pending → compensated (never skipping pending)
- [ ] Assert: `last_phase_transition_at` is monotonically non-decreasing per side_effect

### V2-08 — SV-SESS-04 idempotency key continuity

- [ ] Drive a tool invocation; read /state; capture `side_effects[i].idempotency_key`
- [ ] Force a resume (kill + restart impl)
- [ ] Read /state post-resume; assert same `idempotency_key` value for the same side_effect
- [ ] Tool-side observation: the tool's dedupe cache was consulted with that exact key (if tool-side observability is impl-coupled, this sub-assertion is SKIP with diagnostic "needs tool-side trace surface")

### V2-09 — SV-SESS-01 + SV-SESS-02

- [ ] SV-SESS-01: fetch live /state; validate against `session.schema.json`; trivially passes given V2-05
- [ ] SV-SESS-02: launch impl against a deliberately-corrupted session file (validator writes an invalid `workflow.status` value to the session directory before launch); assert impl refuses to resume with `SessionFormatIncompatible`; process exits non-zero

### V2-10 — SV-SESS-05 non-idempotent tool classification

- [ ] Launch impl with `RUNNER_TOOLS_FIXTURE=<spec>/test-vectors/tool-registry-m2/tools.json` and a filtered sub-fixture containing ONLY `compliant_ephemeral_tool`
  - Assert: impl boots clean; `/permissions/resolve?tool=compliant_ephemeral_tool` returns `Prompt`
- [ ] Launch impl with fixture containing ONLY `non_compliant_ephemeral_tool`
  - Assert: impl exits non-zero within 5s with `ToolPoolStale` reason=`idempotency-retention-insufficient`

### V2-11 — SV-PERM-19 audit-sink three-state degradation

- [ ] For each of `healthy`, `degraded-buffering`, `unreachable-halt`: launch impl subprocess with `SOA_RUNNER_AUDIT_SINK_FAILURE_MODE=<state>`
- [ ] **healthy:** drive N permission decisions; /audit/tail advances; no AuditSink* StreamEvent emitted
- [ ] **degraded-buffering:** drive N decisions; AuditSinkDegraded StreamEvent observed once; /ready stays 200; decisions succeed; /audit/records shows the buffered records with chain intact
- [ ] **unreachable-halt:** drive a ReadOnly decision → 201 (allowed); drive a Mutating decision → expect `PermissionDenied reason=audit-sink-unreachable`; observe `AuditSinkUnreachable` StreamEvent; /ready returns 503 reason=audit-sink-unreachable
- [ ] **Recovery:** start in `unreachable-halt`, then flip env to `healthy` via subprocess restart; assert AuditSinkRecovered emits once; /ready returns 200; buffer flushes in order

### V2-12 — Exit gate

- [ ] All 8 M2 + SV-SESS-STATE-01 green
- [ ] All 15 M1 tests still green (regression pass)
- [ ] Cross-platform matrix on Linux/macOS/Windows (Windows with caveat if impl takes v1.1 escape hatch)
- [ ] Final `release-gate.json` carries per-platform evidence for all 23 test IDs
- [ ] `STATUS.md`: "M2 complete. 23/24 live-green on 3 platforms. HR-02 M3-deferred. Pinned at spec `<final sha>`."

## Deferred

- HR-02 — M3 (§13 Token Budget)
- Any Memory §8 / Budget §13 / UI Gateway / Handoff / Self-Improvement assertions — later milestones

## Pinned fixtures consumed this milestone

| Fixture | Purpose |
|---|---|
| `test-vectors/tool-registry-m2/tools.json` | SV-SESS-05 idempotency classification |
| All M1 fixtures | Regression coverage |

## Schemas loaded

| Schema | Used by |
|---|---|
| `session-state-response.schema.json` | V2-05, V2-06, V2-07, SV-SESS-STATE-01 |
| `session.schema.json` (L-20 version) | SV-SESS-01, V2-09 |
| All M1 schemas | Regression coverage |

## Environment variables validator sets on impl subprocess

| Variable | Purpose |
|---|---|
| `SOA_RUNNER_AUDIT_SINK_FAILURE_MODE` | Drive §10.5.1 state machine for SV-PERM-19 |
| `RUNNER_CRASH_TEST_MARKERS=1` | Request deterministic fsync-boundary markers on stderr |
| `RUNNER_SESSION_DIR=<tempdir>` | Isolate session state per-test for crash-recovery assertions |
| All M1 env vars | Regression coverage (RUNNER_CARD_FIXTURE, RUNNER_TOOLS_FIXTURE, RUNNER_CARD_JWS, RUNNER_INITIAL_TRUST, RUNNER_TEST_CLOCK, SOA_RUNNER_BOOTSTRAP_BEARER) |

## Plan refresh triggers

This file is rewritten when the spec touches:
- §10.5.1 / §10.5.2 / §10.5.3 / §11 / §12.1 / §12.2 / §12.3 / §12.5 / §12.5.1 / §12.5.2
- §15 / §15.5 (HR-* definitions)
- `schemas/session-state-response.schema.json` or `schemas/session.schema.json`
- `test-vectors/tool-registry-m2/` fixtures
- `soa-validate-must-map.json` — M2 test ID milestone/assertion changes

## Daily status protocol

Unchanged from M1 rev 2.
