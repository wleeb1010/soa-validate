# soa-validate — M2 Plan (Crash-Safe Persistence, rev 2 post-evaluator)

**You are in `soa-validate`.** Spec-session-authored plan rev 2 incorporating 16 plan-evaluator findings. Builds on M1 exit (15 pass / 1 skip / 0 fail, pin `8624a7a`).

## Spec pin (M2 rev 2)

| Field | Value |
|---|---|
| spec_commit_sha | `507eeb1` — L-28 M2 rev 2 |
| spec_manifest_sha256 | `0418932a2923452b95f484888f1cbdc64d5a591238d3578644d31abc359f03c0` |
| M2 test set | **16 tests:** `HR-04, HR-05, SV-SESS-01..11, SV-PERM-19, SV-SESS-STATE-01, SV-AUDIT-SINK-EVENTS-01` |
| M1 regression | all 15 M1 IDs must stay green |
| Deferred | HR-02 (§13 Token Budget) → M3; SV-PERM-16/17 (WORM retention + audit-reader access) → M3 |
| **Total M2 exit tally:** 15 M1 + 16 M2 = **31 test IDs** (corrects F-08 off-by-one from rev 1) |

## V2-01 — Pin-bump to `507eeb1`

- [ ] Update `soa-validate.lock`:
  ```
  spec_commit_sha      = 507eeb1b0a12f1a15830e9f826f7af7fa19afb74  (full sha — L-28 F-16 fix: no literal placeholder)
  spec_commit_date     = 2026-04-21
  spec_manifest_sha256 = 0418932a2923452b95f484888f1cbdc64d5a591238d3578644d31abc359f03c0
  pin_history append:
    reason = "adopt L-28 M2 rev 2: 12 plan-evaluator findings resolved
              at spec. §12.5.1 byte-identity contract clarified,
              §12.5.2 concrete-side-effect clarification, new §12.5.3
              crash-marker protocol + §12.5.4 audit-sink-events
              endpoint, side-effect schema fields required, fixture
              inheritance cleanup + filtered sub-fixtures, must-map
              tagged 6 additional SV-SESS as M2 + deferred
              SV-PERM-16/17 to M3 + added SV-AUDIT-SINK-EVENTS-01
              (catalog 222→223)."
  ```

## V2-02 — Schema registry update

- [ ] Load/refresh into validator's schema registry:
  - `schemas/session-state-response.schema.json` — now requires `side_effects.items.{args_digest, first_attempted_at, last_phase_transition_at}` — previously optional
  - `schemas/audit-sink-events-response.schema.json` — **new**
  - `schemas/session.schema.json` (unchanged from M1 L-20)

## Week 1 — Foundation + independent tracks

**V2-03 — V-14 session-state observer (§12.5.1)** — gates V2-05, V2-06, V2-07, V2-08
- [ ] HTTP helper for `GET /sessions/<id>/state` via session bearer (bootstrap grants `sessions:read:<sid>` by default per L-28 F-15 fix)
- [ ] Schema-validate response body
- [ ] Parse `workflow.side_effects[]` with `args_digest + first_attempted_at + last_phase_transition_at` now required
- [ ] Byte-identity predicate (F-01 fix): `strip(body, "generated_at") == strip(body_prior, "generated_at")`. **Do NOT assert raw byte-identity** — `generated_at` is per-request wall-clock timestamp and WILL differ.

**V2-04 — V-15 crash-test harness (gates V2-06)**
- [ ] Extend `internal/subprocrunner` with kill-at-marker:
  - Watch impl stderr for `SOA_MARK_*` named markers per §12.5.3 (impl emits with `RUNNER_CRASH_TEST_MARKERS=1`)
  - Kill -9 / TerminateProcess at named marker
  - Verify subprocess exit
- [ ] Target-marker catalog (pin-bumpable): `PENDING_WRITE_DONE`, `TOOL_INVOKE_START`, `TOOL_INVOKE_DONE`, `COMMITTED_WRITE_DONE`, `DIR_FSYNC_DONE`, `AUDIT_APPEND_DONE`, `AUDIT_BUFFER_WRITE_DONE`
- [ ] Relaunch against same session directory via `RUNNER_SESSION_DIR` isolation; wait for /ready=200
- [ ] Platform-aware: SIGKILL on Linux/macOS, TerminateProcess on Windows. Document Windows limitations (v1.1 if Hyper-V unreliable)

**V2-10 — SV-SESS-05 + SV-SESS-11 non-idempotent classification** — independent, ship Week 1 Day 1
- [ ] Positive path: launch impl subprocess with `RUNNER_TOOLS_FIXTURE=<spec>/test-vectors/tool-registry-m2/tools-compliant-only.json`. Assert impl boots clean; `/permissions/resolve?tool=compliant_ephemeral_tool` returns `Prompt`
- [ ] Negative path: launch with `tools-non-compliant-only.json`. Assert impl exits non-zero within 5s with `ToolPoolStale reason=idempotency-retention-insufficient`
- [ ] Combined fixture: launch with full `tools.json` (2 tools). Assert impl boot refuses on the non-compliant entry

**V2-11 — SV-PERM-19 + SV-AUDIT-SINK-EVENTS-01 (§10.5.1 + §12.5.4)** — independent, ship Week 1 Day 1
- [ ] For each state: launch impl subprocess with `SOA_RUNNER_AUDIT_SINK_FAILURE_MODE=<healthy|degraded-buffering|unreachable-halt>`
- [ ] Poll `GET /audit/sink-events` (§12.5.4 endpoint) — assert exactly one matching `AuditSink*` event per fresh-boot (per L-28 F-13 clarification: fresh boot with env var set emits exactly one state-transition event at boot)
- [ ] `healthy`: drive N decisions; /audit/tail advances; sink-events carries no transition since healthy is the default
- [ ] `degraded-buffering`: drive N decisions; decisions succeed; /audit/records shows buffered records with chain intact; `AuditSinkDegraded` event in /audit/sink-events with `first_failed_at` + `buffered_records` detail
- [ ] `unreachable-halt`: drive a ReadOnly decision → 201; drive a Mutating decision → `PermissionDenied reason=audit-sink-unreachable`; `/ready` returns 503; `AuditSinkUnreachable` event observed

## Week 2 — State observability + crash-recovery

**V2-05 — SV-SESS-STATE-01 live**
- [ ] Mint session via `POST /sessions` (bootstrap now grants `sessions:read:<sid>` scope by default)
- [ ] Drive 2-3 permission decisions via V-07 driver
- [ ] `GET /sessions/<id>/state` — schema-validate; assert `workflow.side_effects.length` matches drive count
- [ ] Every side_effect carries `tool + idempotency_key + phase + args_digest + first_attempted_at + last_phase_transition_at` (required per schema)
- [ ] Byte-identity predicate (excluding `generated_at`): two reads in rapid succession → stripped-body-equal
- [ ] 404 unknown session, 403 wrong bearer, 429 rate-limit, 503 pre-boot regression

**V2-06 — HR-04 + HR-05 crash-recovery via /state + crash markers**
- [ ] **HR-04 (pending replays idempotently):** launch impl with markers. Drive tool invocation. Watch for `SOA_MARK_PENDING_WRITE_DONE`. Read /state; capture `idempotency_key`. Kill impl between `SOA_MARK_PENDING_WRITE_DONE` and `SOA_MARK_COMMITTED_WRITE_DONE`. Relaunch. Read /state. Assert `idempotency_key` preserved. Assert audit chain has exactly one record for this decision (dedupe observed via chain — F-11 fix: no tool-side counter needed).
- [ ] **HR-05 (committed does NOT replay):** same pattern but kill AFTER `SOA_MARK_COMMITTED_WRITE_DONE` + `SOA_MARK_DIR_FSYNC_DONE`. Relaunch. Read /state. Assert `phase=committed` unchanged. Audit chain: still exactly one record (replay did not fire).

**V2-07 — SV-SESS-03 bracket-persist for every significant event**
- [ ] Drive N=10 significant events (mix of permission decisions + tool invocations)
- [ ] Poll /state during drive; capture every phase transition
- [ ] Assert phase transitions always go `pending → committed | compensated` (never skip pending)
- [ ] Assert `last_phase_transition_at` monotonically non-decreasing per side_effect (field now required per schema)

**V2-08 — SV-SESS-04 idempotency key continuity + dedupe (rev 2 wording)**
- [ ] Drive a tool invocation. Read /state. Capture `side_effects[i].idempotency_key`.
- [ ] Kill + restart via V2-04 harness.
- [ ] Read /state post-resume. Assert same `idempotency_key` value.
- [ ] **Dedupe observation (F-11 fix — audit-chain-based, not tool-side):** read /audit/records across the kill boundary. Assert exactly ONE audit row exists for this side_effect's decision — if the tool replayed, it would have produced a second row; single row proves dedupe worked.

## Week 3 — Atomic-write conformance + session-file tamper

**V2-09 — SV-SESS-01 + SV-SESS-02**
- [ ] SV-SESS-01: schema-validate /state response (trivially passes V2-05 work)
- [ ] SV-SESS-02: launch impl against a deliberately-corrupted session file (validator writes invalid `workflow.status` via `RUNNER_SESSION_DIR` isolation). Assert impl refuses to resume with `SessionFormatIncompatible`; process exits non-zero

**V2-09b — SV-SESS-06 + SV-SESS-07 atomic-write conformance (NEW, per F-06 fix)**
- [ ] POSIX atomic-write (SV-SESS-06): using `RUNNER_CRASH_TEST_MARKERS=1`, kill impl between `SOA_MARK_COMMITTED_WRITE_DONE` and `SOA_MARK_DIR_FSYNC_DONE`. Restart. Read session file — should reflect the last fully-flushed state (not a partial write). Run 100 iterations.
- [ ] Windows atomic-write (SV-SESS-07): same pattern on Windows runner. Windows-specific marker ordering verified (same logical boundaries per §12.5.3 cross-platform-identity guarantee).

**V2-09c — SV-SESS-08 + SV-SESS-09 + SV-SESS-10 resume algorithm (NEW, per F-06 fix)**
- [ ] SV-SESS-08 (resume replays pending): overlaps with HR-04 setup; specifically assert resume-algorithm step 4 path
- [ ] SV-SESS-09 (card-drift terminates): kill impl; change Agent Card's `card_version` between invocations; relaunch; assert `StopReason::CardVersionDrift`
- [ ] SV-SESS-10 (inflight compensation): kill impl at `SOA_MARK_TOOL_INVOKE_START` (side_effect is in-flight with no result_digest). Relaunch. Assert either compensating action fires OR `ResumeCompensationGap` is tagged per §12.5 step 4

## M2 exit gate — three-phase (per structural challenge #9)

**Week 1 exit (structural + state-machine):** V2-10 + V2-11 green → SV-SESS-05, SV-SESS-11, SV-PERM-19, SV-AUDIT-SINK-EVENTS-01 passing. V2-05 green → SV-SESS-STATE-01 passing. 5 M2 IDs live.

**Week 2 exit (crash-recovery core):** V2-06 + V2-07 + V2-08 green → HR-04, HR-05, SV-SESS-03, SV-SESS-04 passing. 4 more M2 IDs live (total 9).

**Week 3 exit (full matrix):** V2-09a/b/c green → SV-SESS-01, SV-SESS-02, SV-SESS-06, SV-SESS-07, SV-SESS-08, SV-SESS-09, SV-SESS-10 passing. Final 7 M2 IDs live.

**M2 complete:** 16 M2 + 15 M1 = **31 test IDs live-green** across Linux + macOS + Windows (Windows with v1.1 caveat if escape hatch taken). Final `release-gate.json` + JUnit with per-platform evidence for all 31 IDs.

## Deferred

- HR-02 — M3 (§13 Token Budget)
- SV-PERM-16, SV-PERM-17 — M3 (WORM retention + audit-reader access; formally tagged in must-map per L-28 F-05)
- Any Memory §8 / Budget §13 / UI Gateway / Handoff / SI — later milestones

## Env vars validator sets on impl subprocess

| Variable | Purpose |
|---|---|
| `SOA_RUNNER_AUDIT_SINK_FAILURE_MODE` | Drive §10.5.1 state machine for SV-PERM-19 (§12.5.2) |
| `RUNNER_CRASH_TEST_MARKERS=1` | Enable stderr marker emission for V-15 kill-at-marker (§12.5.3) |
| `RUNNER_SESSION_DIR=<tempdir>` | Per-test session isolation (§12.5.3) |
| All M1 env vars | Regression coverage |

## Schemas loaded

| Schema | Usage |
|---|---|
| `session-state-response.schema.json` | V2-03, V2-05, V2-06, V2-07, V2-08, V2-09, SV-SESS-STATE-01 |
| `audit-sink-events-response.schema.json` | V2-11, SV-AUDIT-SINK-EVENTS-01 |
| `session.schema.json` (L-20) | SV-SESS-01, V2-09 |
| All M1 schemas | Regression coverage |

## Plan refresh triggers

Unchanged except add §12.5.3 + §12.5.4 and the two new M2 schemas to the trigger list.

## Daily status protocol

Unchanged from M1 rev 2.
