# soa-validate — M3 Plan rev 2 (post-evaluator resolution)

**You are in `soa-validate`.** Spec-session-authored plan rev 2 resolving 15 findings + 6 structural challenges from sibling-plan evaluator pass.

## Spec pin

| Field | Value |
|---|---|
| spec_commit_sha | `5e97277` — L-34 M3 sibling-plan evaluator resolution |
| **full SHA (per F-05)** | `5e97277<paste git-rev-parse result>` — **run `git rev-parse 5e97277` in your spec clone and paste the full 40-char result here. Do NOT paste a placeholder.** |
| spec_manifest_sha256 | `d211c3c94436fb43afc5c616284f35b6993d2fdfb9bcd45af09a8696aef3103e` |
| M3 test set | **139 tests** (was 145; 4 SV-CLUS → M4, 2 SV-GOV → M5 per L-34) — target ≥120 green, **19-test skip budget** |
| Regression | 22 M2 + 1 M1 retro must stay green |
| Total exit tally | **139 M3 + 22 M2 + 1 M1 retro = 162 test IDs** across 3 platforms (F-04 corrected) |

## Pre-budgeted skips (count against the 19 budget, not green)

| Test | Skip reason |
|---|---|
| SV-STR-04 | §14.3 SSE terminal-event semantics — `/events/recent` polling may not exercise terminal-event ordering |
| SV-GOV-09 | JSON-RPC -32061 needs A2A (M5) |
| SV-MEM-08 | Cross-tenant isolation may need real Memory MCP |
| HR-13 | ToolPoolStale resume correlation — observation gap if impl uses digest-only correlation |

## V-1 Pin-bump to `5e97277`

- [ ] `git rev-parse 5e97277` in your spec clone; paste the full 40-char SHA into the lock file. **Do not use a placeholder literal — validate before committing. F-05 previously shipped `f05f950d9c0c4...` (wrong); always use rev-parse output.**
- [ ] `soa-validate.lock` update:
  ```
  spec_commit_sha      = <full sha from rev-parse>
  spec_manifest_sha256 = d211c3c94436fb43afc5c616284f35b6993d2fdfb9bcd45af09a8696aef3103e
  pin_history append:
    reason = "M3 kickoff + rev 2 + L-34 sibling-plan evaluator
              resolution: §8.3.1 MemoryDegraded clarification,
              §11.3.1 dynamic-tool-add test hook, memory-mcp-mock
              fixture, SV-CLUS M3→M4 retag, SV-GOV-10/12 M3→M5 retag.
              139 M3 tests with 19-test skip budget (4 pre-budgeted)."
  ```

## V-2 Schema registry update

- [ ] Load: `memory-state-response`, `budget-projection-response`, `tools-registered-response`, `events-recent-response`
- [ ] Confirm M1/M2 schemas still loaded

## V-3 Platform-matrix CI scaffold + wall-clock baseline `[wall-clock baseline per F-11]`

- [ ] Extend GH Actions matrix for 162-test scale
- [ ] **Run a baseline measurement on M2's 31-test suite now** — record per-platform wall-clock. Target ≤5min Linux / ≤8min Windows for 162 tests is a projection from M2's 31-test baseline × 5.2× scale + subprocess-overhead estimates; validate projections against the M2 baseline before relying on them
- [ ] If the baseline shows M2 already exceeds (162 / 31) × budget, raise to spec session before Week 2

## V-4 SV-MEM + SV-MEM-STATE handlers (10 tests)

- [ ] Scaffold handlers per must-map
- [ ] SV-MEM-STATE-01 schema + SV-MEM-STATE-02 byte-identity (excl `generated_at`)
- [ ] **Consume `test-vectors/memory-mcp-mock/` per L-34.** Start the mock with controlled corpus-seed; drive impl through seeded-query scenarios
- [ ] SKIP until impl T-0 + T-1 ship

## V-5 SV-STR + SV-STR-OBS handlers — 14 tests (corrected count per F-03)

- [ ] SV-STR-01..11 + SV-STR-15 + SV-STR-16 (13) + SV-STR-OBS-01 (1) = 14
- [ ] Consume via `GET /events/recent` (impl T-2)
- [ ] **HR-17 uses `SessionEnd{stop_reason:MemoryDegraded}` per §8.3.1 — do NOT look for a bare MemoryDegraded event type; it isn't in the §14.1 25-type enum (F-01)**
- [ ] SKIP until impl T-2 ships

**Week 1 exit:** 24 M3 handlers wired (10 + 14); platform-matrix baseline measured; honest SKIPs until impl ships Week-1 tasks.

## Week 2

### V-6 SV-BUD + SV-BUD-PROJ — 9 tests

- [ ] SV-BUD-01..07 + SV-BUD-PROJ-01/02
- [ ] SKIP until impl T-4

### V-7 SV-REG + SV-REG-OBS — 7 tests

- [ ] SV-REG-01..05 + SV-REG-OBS-01/02
- [ ] **SV-REG-03 uses the §11.3.1 `SOA_RUNNER_DYNAMIC_TOOL_REGISTRATION` env-var hook per L-34** — validator writes a tool spec to the trigger file; impl ingests and updates `/tools/registered`; in-flight session's `tool_pool_hash` stays unchanged
- [ ] SKIP until impl T-5

### V-8 SV-HOOK — 8 tests

- [ ] SV-HOOK-01..08 via `internal/subprocrunner`
- [ ] SKIP until impl T-6

**Week 2 exit:** 24 more handlers; running total 48 M3.

## Week 3 — Core completion (split per S-6) + Week-4 pre-wire

### V-9a SV-CARD + SV-BOOT remainders (17 tests)

- [ ] SV-CARD-02..12 (11) — **count corrected; must-map has SV-CARD-01 as M3-tagged but impl shipped it in M1, so effectively 11 new** (per F-03 corrected tally)
- [ ] SV-BOOT-02..07 (6) — subprocess harness variations

### V-9b SV-PERM remainder — 21 tests (per F-03 corrected)

- [ ] SV-PERM-02..22 — three-axis depth, precedence, escalation, audit linkage, key rotation

### V-9c SV-SIGN remainder — 5 tests (per F-03 corrected)

- [ ] SV-SIGN-02..06

### V-10 Policy/manifest handlers — 16 tests

- [ ] SV-ENC (7) + SV-PRIN (5) + SV-STACK (2) + SV-OPS (2)

### V-11 SV-AGENTS remainder — 7 tests

### V-12 Testable-today HRs — 6 tests

- [ ] HR-01, HR-07, HR-09, HR-10, HR-11, HR-13

### V-12b Pre-wire Week-4 handlers (new per S-3) — against impl T-11b scaffolds

- [ ] Wire SV-GOV (10) + SV-PRIV (5) handlers against placeholder endpoints shipped in impl T-11b
- [ ] All SKIP until impl T-12 swaps placeholders for real behavior

**Week 3 exit:** 68 more handlers + 15 scaffold-wired; running total 131 M3 handlers wired.

## Week 4

### V-13 Governance + privacy live-flips (Week 3 pre-wire becomes live) — 15 tests

- [ ] SV-GOV-01..08, 11 (10 tests — removing M5-retagged -10/-12)
- [ ] SV-PRIV-01..05

### V-14 Endpoint-dependent HRs — 4 tests

- [ ] HR-02: Projection over budget → observe via `GET /events/recent` for `SessionEnd` with `payload.stop_reason: "BudgetExhausted"`
- [ ] HR-03: Mid-stream — observe `SessionEnd{stop_reason:BudgetExhausted}` during active decision
- [ ] HR-06: Budget + cache-accounting correlation via `/budget/projection`
- [ ] HR-17: drive memory-mcp-mock with `SOA_MEMORY_MCP_MOCK_TIMEOUT_AFTER_N_CALLS=0`; observe `SessionEnd{stop_reason:MemoryDegraded}` via `/events/recent` per §8.3.1

### V-15 Exit-gate consolidation

- [ ] `soa-validate --profile=core` full run; JUnit + `release-gate.json` for 162 IDs across 3 platforms
- [ ] Per-platform skip check: ≤ 19 skips (4 pre-budgeted + up to 15 real slips)
- [ ] STATUS final entry: "M3 complete. ≥120 M3 + 22 M2 + 1 M1 retro = ≥143 total live-green."

## Exit gate

- [ ] ≥120 M3 + 22 M2 + 1 M1 retro = **≥143 total across 3 platforms**
- [ ] ≤ 19 skips per platform (4 pre-budgeted; remainder documented)
- [ ] Zero fails, zero errors

## Env vars this validator sets on impl subprocess

M3 additions:
- `SOA_RUNNER_DYNAMIC_TOOL_REGISTRATION=<file>` — SV-REG-03
- `SOA_MEMORY_MCP_MOCK_TIMEOUT_AFTER_N_CALLS` / `...RETURN_ERROR` / `...SEED` — SV-MEM + HR-17

Inherits all M1/M2 env vars.

## STATUS.md machine-readable block (per S-4)

Validator emits same schema as impl for symmetric parsing:

```
<!-- machine-readable -->
{
  "week": 2,
  "v_tasks_landed": ["V-6","V-7","V-8"],
  "scoreboard": {"pass": 48, "skip": 91, "fail": 0, "error": 0},
  "pre_budgeted_skip_count": 4,
  "real_slip_count": 0,
  "spec_pin": "5e97277"
}
<!-- /machine-readable -->
```

## Plan refresh triggers

Spec-session rewrites this file when §8.*, §11.*, §13.*, §14.*, §15, or any M3 schema/fixture changes.

## Daily status protocol

Unchanged + the machine-readable block above.
